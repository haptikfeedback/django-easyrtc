# django_easyrtc

Clone this repo, and install the dependencies!

## Node Install:
* `cd rtc_server`
* `npm install`

## Django Install:
* `cd django_project`
* `python -m venv env`
* `pip install -r requirements.txt`

## Run The Servers:
* `./start.sh`
* *And you're ready to go!*